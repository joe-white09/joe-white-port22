import ProjectList from '../ProjectList';

function Portfolio() {
    return(
        <section id='projects' >
            <h1 className='section'>Portfolio</h1>
            <ProjectList />
        </section>
    );
}

export default Portfolio;